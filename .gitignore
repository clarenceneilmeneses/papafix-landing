# Play screenshot pages are generated by marketing/sources/build-play-*.js — about 15 MB
# of base64-embedded fonts and captures. Rebuild them, don't track them. (The feature
# graphic and print HTML in the same folder IS hand-authored, and stays tracked.)
marketing/sources/PapaFix_PLAY_Shot_*.html
marketing/sources/PapaFix_PLAY_Tablet_*.html

# Another company's Play listing, kept locally as a design reference. Their artwork,
# not ours — fine to look at, not something to publish in our repo.
googleplaypromo/tarsi reference/
